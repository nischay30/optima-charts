apiVersion: v1
kind: Service
metadata:
  name: {{ include "optima.fullname" . }}
  labels:
    {{- include "optima.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "optima.selectorLabels" . | nindent 4 }}
