apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "optima.fullname" . }}-test-connection"
  labels:
    {{- include "optima.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "optima.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
